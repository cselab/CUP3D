CC=CC
LD=CC

ACCFFTDIR=../accfft/build/
accfft-inc = $(ACCFFTDIR)/include/
accfft-lib = $(ACCFFTDIR)/lib/
accfft?=true

ifeq ($(INTEL_LICENSE_FILE),)
CPPFLAGS += -dynamic
else
CPPFLAGS += -static
endif

ifeq "$(hypre)" "true"
CPPFLAGS+= -I/users/novatig/hypre/build/include/
LIBS+= -L/users/novatig/hypre/build/lib/
endif

LIBS += $(CRAY_CUDATOOLKIT_POST_LINK_OPTS)

CPPFLAGS += -DCUP_ASYNC_DUMP
